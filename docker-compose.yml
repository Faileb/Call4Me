version: '3.8'

services:
  call4me:
    build: .
    container_name: call4me
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      # Twilio (required)
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
      - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}
      # App (required)
      - APP_SECRET=${APP_SECRET}
      - APP_PASSWORD=${APP_PASSWORD}
      - APP_BASE_URL=${APP_BASE_URL}
      # Database
      - DATABASE_URL=file:/app/data/call4me.db
      # Storage
      - RECORDINGS_PATH=/app/data/recordings
      # Optional
      - APP_PORT=3000
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=production
      - TZ=${TZ:-UTC}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

# Optional: Add PostgreSQL if you prefer it over SQLite
#  postgres:
#    image: postgres:16-alpine
#    container_name: call4me-db
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#    environment:
#      - POSTGRES_USER=call4me
#      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
#      - POSTGRES_DB=call4me
#    restart: unless-stopped
#
#volumes:
#  postgres_data:
