# Call4Me Environment Configuration
#
# NOTE: Twilio credentials, password, and base URL are best configured via
# the web-based setup wizard. These env vars are fallbacks for development
# or migration from older versions.

# Database (defaults to SQLite)
DATABASE_URL=file:./prisma/data/call4me.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/call4me

# App configuration
APP_PORT=3000
APP_SECRET=your-secure-secret-key-at-least-32-chars
NODE_ENV=development

# Storage
RECORDINGS_PATH=./data/recordings

# Optional settings
LOG_LEVEL=info
TZ=America/New_York

# =============================================================================
# The following are optional - prefer using the setup wizard instead
# =============================================================================

# Twilio credentials (optional - use setup wizard instead)
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token_here
# TWILIO_PHONE_NUMBER=+1234567890

# Auth (optional - use setup wizard instead)
# APP_PASSWORD=your_secure_password
# APP_BASE_URL=http://localhost:3000
# DISABLE_AUTH=false
